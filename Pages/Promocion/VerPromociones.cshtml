@page
@model CRM.Pages.VerPromocionesModel
@{
    ViewData["Title"] = "PROMOCIONES";
}

<a href="~/Promocion/AgregarPromocion" class="btn btn-primary btn-lg btn-block mt-3 mb-5">Agregar Promoción</a>

@if (Model.Promociones.Count > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Tratamiento</th>
                <th>Descripción</th>
                <th>Precio Nuevo</th>
                <th>Estatus</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var promo in Model.Promociones)
            {
                <tr>
                    <td>@promo.Tratamiento</td>
                    <td>@promo.Descripcion</td>
                    <td>@promo.PrecioNuevo</td>
                    <td>@promo.Estatus</td>
                    <td>
                        <a asp-page="EditarPromocion" asp-route-id="@promo.Id" class="btn btn-warning btn-sm">Editar</a>
                        <form method="post" asp-page-handler="Eliminar" asp-route-id="@promo.Id" style="display:inline;">
                            <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                        </form>
                        <a asp-page="/Promociones/DetallesPromocion" asp-route-promocionId="@promo.Id" class="btn btn-info">Enviar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No hay promociones registradas.</p>
}

